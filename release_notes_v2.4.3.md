# SOMA Vision Metrology V2.4.3

## 主要更新

- 新增独立“图像查看器”：单图显示单个画布，双图模式并排显示上层和下层画布。
- 查看器为只读视图，保留当前 ROI、识别轮廓和几何标注，支持滚轮缩放、中键或空格拖动、适应窗口及 `1:1` 实际像素查看。
- 新增本机“最近文件”列表，可快速重新导入上层/单图或下层图像；无效路径自动跳过，记录可手动清除。
- 最近文件移至顶栏并放在当前配方左侧；配方管理、导入配方和保存当前配方统一收进“当前配方”菜单。
- 自动候选精测继续遵循右侧 ROI 类型；椭圆 ROI 保留椭圆拟合、圆度及长短轴显示。
- 修复切换到双图模式后导入下层图像可能被错误写入上层的状态同步问题。

## 验证

- 完整测试：138 项通过。
- 已验证单图和双图独立查看器、最近文件菜单及 V2.4.3 Windows Setup 启动检查。

## 安装

运行 `SOMA_Vision_Metrology_V2.4.3_Setup.exe` 完成安装或覆盖升级。安装包的 SHA256 校验值见同名 `.sha256` 文件及 `update_manifest.json`。
