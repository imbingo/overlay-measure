# SOMA Vision Metrology V2.4.6

## 批量导入配对状态

- 批量导入表新增“配对”列，不改变原有导入路径或批量执行逻辑。
- 双图模式会显示每个 Mark 的已配对组数；上下层数量不一致时显示未配对数量和“需检查”。
- 单图模式明确显示“单图，无需配对”。
- 配对继续按已导入列表的自然排序逐张进行；数量不一致时仍禁止批量运行。

## 图标设计规则

- 功能图标固定采用简约、低对比、辅助文字理解的样式。
- 禁止使用大面积黑色、拟物化或抢占按钮文字层级的图标。
- 顶部命令栏维持 V2.2.4 的文字与简单符号风格。

## 验证

- 全量自动化测试：142 项通过。
- Qt 离屏窗口已用双图批量的“已配对 / 未配对”状态完成界面检查。
