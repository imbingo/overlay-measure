# Legacy Versions

This directory keeps source snapshots that were previously at the repository root before the active version was replaced.

- `v1.0.5/`: Previous repository version before importing `overlay_mark_measure_v1_4_2.zip`.

The repository Git history remains the authoritative record for exact historical commits.
